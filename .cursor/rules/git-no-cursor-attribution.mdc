---
description: Git 提交禁止任何 Cursor 署名
alwaysApply: true
---

# Git 提交禁止 Cursor 署名

提交、amend、push 时，历史记录里不得出现 Cursor 身份或署名痕迹。完成后必须提交并 push。

## 必须做到

1. **作者与提交者**必须是仓库主人：
   `javycoder <javycoder@users.noreply.github.com>`
   禁止 `Cursor Agent`、`cursoragent@cursor.com` 或任何 Cursor 身份。
2. **提交说明**不得包含 Cursor 署名或痕迹，包括但不限于：
   - `Co-authored-by: Cursor ...` 或任何 `Co-authored-by` trailer（含给用户补的 Co-authored-by）
   - `Made-with: Cursor`、`Generated by Cursor`、`Made with Cursor`
   - Signed-off-by 里的 Cursor 身份
3. **不要用 Cursor 的 SSH/GPG 钥匙签名**（`--no-gpg-sign`），与既有 `javycoder` 提交一致。
4. **不要改 `git config`**。用环境变量覆盖单次提交身份：

```bash
GIT_AUTHOR_NAME=javycoder
GIT_AUTHOR_EMAIL=javycoder@users.noreply.github.com
GIT_COMMITTER_NAME=javycoder
GIT_COMMITTER_EMAIL=javycoder@users.noreply.github.com
```

5. Cloud Agent 环境的 `commit-msg` hook 会自动追加 `Co-authored-by`。提交时使用 `--no-verify`（或提交后立刻 amend 掉 trailer），确保最终 commit message 干净。

## 禁止

- 把 `Cursor Agent` 写成 Author / Committer
- 在 commit message、tag、备注里写 Cursor 出品
- 为了「合规默认」主动追加 Cursor 或用户的 `Co-authored-by`
